# Tasks: Unified Filter Synchronization

- [x] T016 Add auto-trigger (debounce) for the `directionSelect` dropdown in `src/static/js/graph.js`.
- [x] T017 Add auto-trigger (debounce) for `damping-factor` and `iterations` inputs in `src/static/js/graph.js` with a 500ms delay.
- [x] T018 Verify that all form inputs correctly contribute to the `payload` in `triggerAnalysis()`.
- [x] T019 Update the reset button logic to also clear the `directionSelect` and numeric inputs to their defaults.
- [x] T020 Final end-to-end verification of filter combinations (e.g., League + Season + Direction).
